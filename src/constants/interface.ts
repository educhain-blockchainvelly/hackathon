export interface TestInterface {
          someKey: string;
          anotherKey: number;
        }
        
        export interface NetworkConfig {
          testInfo: TestInterface;
          rpcUrl: string;
          chainId: number;
        }
        